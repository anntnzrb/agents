# Example Flows

## Quick lookup
1. `search` with a short query and small `-n`.
2. If a result looks promising, use `content` on the URL.

## Quick scan
1. `search` with `--content` for a small `-n`.
2. Skim the embedded content.
