# Tooling Reference

Short constraints and defaults for each CLI command.

## search
- Command: `search.ts <query>`
- Options: `-n <num>`, `--content`
- Notes: returns snippet-only by default; `--content` fetches page text.

## content
- Command: `content.ts <url>`
- Notes: extracts readable markdown from a single URL.
