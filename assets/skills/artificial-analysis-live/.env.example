# Template only. Prefer a process-injected key; if a dotenv file is required,
# keep a filled copy outside the skill tree with restrictive permissions (0600).
# Never create a filled skill-local `.env` or sync one into generated homes.
ARTIFICIAL_ANALYSIS_API_KEY=
# Optional override
# ARTIFICIAL_ANALYSIS_API_BASE_URL=https://artificialanalysis.ai/api/v2/data/llms/models
