{
  "skill_name": "deepswe-live",
  "evals": [
    {
      "id": 1,
      "prompt": "Prepare a current DeepSWE model-efficiency report. Compare every published model configuration on pass@1, mean output tokens, mean cost in USD, and mean agent steps. Show counts and confidence-interval width, identify the best trade-offs, and keep raw extrema separate from recommendations.",
      "expected_output": "A fresh, JSON-grounded metrics report that preserves configuration identity, labels published versus derived values, reports uncertainty as CI width, and presents Pareto-efficient choices without inventing a composite score.",
      "files": [],
      "expectations": [
        "Resolves one benchmark version deterministically and runs the report route (fetching the published leaderboard first when currentness matters).",
        "Keeps model, reasoning_effort, harness, and config as one configuration identity and never merges tiers that share a model name.",
        "Includes n_attempted and n_tasks_attempted when supplied, pass/score fields, ci_lo, ci_hi, ci_half, and derived ci_width for ranked rows.",
        "Separates raw extrema from filtered recommendations and keeps source fields unchanged under a derived label.",
        "Computes Pareto using maximize pass_at_1 and minimize mean_output_tokens, mean_cost_usd, and mean_agent_steps; excludes nulls and does not use an arbitrary composite score.",
        "Cites benchmark_version, source URL, fetched_at, generated_at when supplied, and validator metadata when available."
      ]
    },
    {
      "id": 2,
      "prompt": "Give me the DeepSWE efficiency recommendation twice: first with the default quality policy, then excluding configurations with fewer than 100 attempted rollouts. Explain exactly what changed and do not silently hide low-sample rows.",
      "expected_output": "Two comparable JSON-derived recommendation views with explicit filters, counts, CI widths, and a clear statement that the second view applies an opt-in sample threshold while raw extrema remain separate.",
      "files": [],
      "expectations": [
        "Uses report or rank with no quality/sample exclusion in the first view and --min-attempted 100 only in the second.",
        "Exposes filters_applied and recommendation counts for both views; does not imply that default recommendations are statistically screened.",
        "Preserves each full configuration identity and published score/pass fields in both outputs.",
        "Marks calculations such as ci_width and threshold-dependent rankings as derived rather than publishing them as source values.",
        "States provenance and freshness for the selected benchmark version and reports an error instead of substituting an unrequested stale artifact."
      ]
    },
    {
      "id": 3,
      "prompt": "For a metrics audit, summarize the raw DeepSWE trials included in the published score. Use the default source, full evaluation scope, and included-only filter, then tell me how visibility changes if I explicitly request all trial rows. Do not describe task contents.",
      "expected_output": "A metrics-only JSON response that distinguishes published_raw trial values from derived counts, states both filter sets, and makes the broader raw scope visible without calling every trials.json row full DeepSWE.",
      "files": [],
      "expectations": [
        "Uses trials with source=deep-swe, eval_scope=full, and included_in_score=true by default and reports filters_applied.",
        "Uses an explicit inclusion override for the broader view and reports the resulting count/visibility change rather than silently widening defaults.",
        "Labels raw trial values published_raw and any counts or summaries derived; never overwrites source fields.",
        "Keeps the response metrics/results-only: no task, exercise, release, trajectory, or trial-artifact downloads or details.",
        "Includes benchmark_version and provenance URL/fetched_at and does not require live network access when an explicit snapshot is supplied."
      ]
    },
    {
      "id": 4,
      "prompt": "Compare two local DeepSWE leaderboard snapshots for model-efficiency changes. Use pass_at_1 as the primary delta, retain configuration identity and provenance, and reject the comparison if the snapshots are from different benchmark versions.",
      "expected_output": "A deterministic JSON comparison with before/after values and deltas for the same benchmark version, or a structured mixed-version error; no guessed release or cross-version ranking.",
      "files": ["<left-snapshot>", "<right-snapshot>"],
      "expectations": [
        "Uses compare with explicit snapshots and confirms both benchmark_version values before computing deltas.",
        "Reports before, after, and delta as derived values while retaining published source values and complete configuration identity.",
        "Carries snapshot freshness and source provenance in the response; never presents local historical data as current.",
        "Returns the JSON error envelope with a mixed-version code when versions differ and does not fall back to latest or v1."
      ]
    },
    {
      "id": 5,
      "prompt": "Using the deterministic evidence fixture, audit known and unsafe DeepSWE metric values. Explain raw versus normalized values, published versus derived status, semantic/eligibility blockers, and prove that no task content or fixed live counts are needed.",
      "expected_output": "A fixture-backed metrics-only audit that preserves source fields, carries numeric evidence and statuses, leaves unsafe values visible but unranked, and reports stable redacted diagnostics.",
      "files": [
        "tests/fixtures/phase6/evidence.json"
      ],
      "expectations": [
        "Every cited known value includes raw/normalized/unit/source-path/parser/hash evidence when available.",
        "Missing, placeholder, malformed, non-finite, ambiguous, unknown, and out-of-range values remain visible with semantic or eligibility reasons and are not silently ranked.",
        "Published, published_raw, and derived labels remain distinct; derived fields never overwrite source fields.",
        "diagnose is offline, emits stable diagnostics, redacts credentials, and never emits task, exercise, release, trajectory, or trial-artifact content.",
        "The fixture-backed evaluation makes no assertion about current live row counts."
      ]
    },
    {
      "id": 6,
      "prompt": "Compare the two deterministic schema fixtures with strict DeepSWE semantics. Explain the same-version requirement, schema-diff blocking, complete duplicate identity behavior, immutable cache/release policy, and the configured latest rule without discovering releases.",
      "expected_output": "A fixture-backed strict comparison that blocks incompatible schema before deltas, preserves legacy before/after/delta keys, and documents diagnostics, cache, stale/snapshot, and release-authority rules.",
      "files": [
        "tests/fixtures/phase6/schema-left.json",
        "tests/fixtures/phase6/schema-right.json"
      ],
      "expectations": [
        "Uses explicit local fixture paths and one concrete benchmark version; no release discovery or network is required.",
        "strict-compare blocks schema/semantic incompatibility and never imputes before, after, or delta values.",
        "Identity is the complete model/reasoning_effort/harness/config tuple; identical duplicates warn and conflicting duplicates warn by default but block strict compare/rank.",
        "Immutable SHA-256 bytes/manifests and non-destructive legacy promotion are documented; refresh failures require explicit --allow-stale or --snapshot.",
        "latest follows only DEEPSWE_DEFAULT_VERSION or the configured default, with dependencies []/unknown when no claims exist."
      ]
    }
  ]
}
