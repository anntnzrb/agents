# Optional primary pricing source. Create a personal key at https://gg.deals/api/
GG_DEALS_API_KEY=

# Optional enrichment source; only used with lookup --include-itad.
ITAD_API_KEY=

# Optional explicit dotenv path. Process variables still take precedence.
GAME_DEALS_ENV_FILE=
