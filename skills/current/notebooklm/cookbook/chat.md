# Chat Cookbook

## List saved chat sessions

```bash
nlm chat-list
```

Sessions stored per notebook ID.

## Interactive chat

```bash
nlm chat <notebook-id>
```

In chat: `/help`, `/history`, `/reset`, `/save`, `/multiline`, `/exit`.
