# =============================================================================
# Models
# =============================================================================
modelRoles:
  default: openai-codex/gpt-5.6-sol:high
  task: openai-codex/gpt-5.6-luna:max
  fast_worker: opencode-go/deepseek-v4-flash:max
  good_worker: openai-codex/gpt-5.6-luna:max

  commit: google-antigravity/gemini-3.6-flash:high # luna: high
  smol: openai-codex/gpt-5.6-luna:max
  tiny: openai-codex/gpt-5.6-luna:off

  plan: openai-codex/gpt-5.6-luna:max
  advisor: opencode-go/deepseek-v4-flash:max
  slow: openai-codex/gpt-5.6-luna:max

# =============================================================================
# Advisor
# =============================================================================
advisor:
  enabled: true
  syncBacklog: "3"
  immuneTurns: 3
  subagents: false
# =============================================================================
# UI / Interaction
# =============================================================================
theme:
  dark: titanium
  light: light
hideThinkingBlock: true
inlineToolDescriptors: auto
collapseChangelog: true
followUpMode: all
steeringMode: all
interruptMode: wait
doubleEscapeAction: tree
treeFilterMode: no-tools
personality: pragmatic
textVerbosity: low
tui:
  textSizing: true
display:
  showTokenUsage: true

# =============================================================================
# MCP / Memory
# =============================================================================
mcp:
  enableProjectConfig: false
  notifications: false
memory:
  backend: off
mnemopi:
  autoRetain: false
  llmMode: none

# =============================================================================
# Context / Branching
# =============================================================================
branchSummary:
  enabled: true
  reserveTokens: 16384
compaction:
  enabled: true
  midTurnEnabled: true
  strategy: snapcompact
  thresholdPercent: 80
  remoteEnabled: true
  supersedeReads: true
  dropUseless: true
# =============================================================================
# Tools
# =============================================================================
edit.mode: hashline

checkpoint:
  enabled: true

# =============================================================================
# Eval kernels
# =============================================================================
# eval:
#   py: true
#   js: false
#   rb: false
#   jl: false

browser:
  enabled: true
  headless: true
  cmux: false

# =============================================================================
# Task (Subagents)
# =============================================================================
task:
  eager: preferred
  batch: true
  prewalk: false
  maxConcurrency: 8
  enableLsp: false
  maxRecursionDepth: 2
  agentModelOverrides:
    sonic: "@fast_worker"
    task: "@good_worker"
  maxRuntimeMs: 3600000
  isolation:
    mode: auto
    apply: false
    merge: branch
    commits: ai
async:
  enabled: true
  maxJobs: 8

features:
  unexpectedStopDetection: true

# =============================================================================
# Code Intelligence
# =============================================================================
lsp:
  enabled: true
  lazy: true
  formatOnWrite: false
  diagnosticsOnWrite: true
  diagnosticsOnEdit: false
  diagnosticsDeduplicate: true

# =============================================================================
# Bash Guardrails
# =============================================================================
bash:
  autoBackground:
    enabled: false

bashInterceptor:
  enabled: true
  patterns:
    - pattern: "(?:^|\\s*(?:&&|\\|\\||;)\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:echo|printf)\\s+.*(?:>|>>)\\s*(?:\"[^\"]+\"|'[^']+'|[^\\s\"']+)(?:\\s*(?:&&|\\|\\||;).*)?$|(?:^|\\s*(?:&&|\\|\\||;)\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*cat\\s*<<[^\\n]*>\\s*(?:\"[^\"]+\"|'[^']+'|[^\\s\"']+)"
      tool: write
      message: "Use the `write` tool instead of echo/cat redirection. It handles encoding and provides confirmation."
    - pattern: "(?:^|\\s*(?:&&|\\|\\||;)\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(cat|head|tail|less|more)\\s+"
      tool: read
      message: "Use the `read` tool instead of cat/head/tail for direct file reads. Shell pipelines may use head/tail to trim command output."
    - pattern: "(?:^|\\s*(?:&&|\\|\\||;)\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(grep|rg|ripgrep|ag|ack)\\s+"
      tool: grep
      message: "Use the `grep` tool instead of grep/rg for file searches. Shell pipelines may use grep to filter command output."
    - pattern: "(?:^|\\s*(?:&&|\\|\\||;)\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:fd\\b|locate\\s+.*(?:-name|-iname|-type|--type|-glob)|find\\s+(?![^\\n;|&]*(?:-mtime|-mmin|-ctime|-cmin|-atime|-amin|-size|-exec|-execdir|-delete|-prune|-perm|-user|-group|-newer|-samefile)\\b)[^\\n;|&]*(?:-name|-iname|-type|--type|-glob))"
      tool: glob
      message: "Use the `glob` tool for simple find/fd file discovery. Shell find is allowed for advanced predicates/actions like -mtime, -size, or -exec."
    - pattern: "(?:^|\\s*(?:&&|\\|\\||;)\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*sed\\s+(-i|--in-place)"
      tool: edit
      message: "Use the `edit` tool instead of sed -i. It provides diff preview and fuzzy matching."
    - pattern: "(?:^|\\s*(?:&&|\\|\\||;)\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*perl\\s+.*-[pn]?i"
      tool: edit
      message: "Use the `edit` tool instead of perl -i. It provides diff preview and fuzzy matching."
    - pattern: "(?:^|\\s*(?:&&|\\|\\||;)\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*awk\\s+.*-i\\s+inplace"
      tool: edit
      message: "Use the `edit` tool instead of awk -i inplace. It provides diff preview and fuzzy matching."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:pythonw?(?:[23])?(?:\\.\\d+)*(?:\\.exe)?|py(?:\\.exe)?|pypy(?:3)?(?:\\.exe)?)\\b"
      flags: i
      tool: bash
      message: "Direct Python execution is disabled. Load the active language skill selected from the task artifacts and repository, then use uv workflow (`uv run`, `uv run --with`, `uv add`)."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:pip(?:[23])?(?:\\.\\d+)*(?:\\.exe)?|pipx(?:\\.exe)?)\\b"
      flags: i
      tool: bash
      message: "pip/pipx is disabled. Load the active language skill selected from the task artifacts and repository, then use uv workflow (`uv add`, `uv pip`, `uv run --with`, `uv tool`)."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:pip-compile|pip-sync|pipdeptree)(?:\\.exe)?\\b"
      flags: i
      tool: bash
      message: "pip-tools style commands are disabled. Load the active language skill selected from the task artifacts and repository, then use `uv pip compile`, `uv pip sync`, or uv-native project commands."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:conda|hatch|mamba|micromamba|pdm|pipenv|poetry|rye|virtualenv|pyenv)(?:\\.exe)?\\b"
      flags: i
      tool: bash
      message: "Python env/package/version tooling is disabled. Load the active language skill selected from the task artifacts and repository, then use uv workflow (`uv python`, `uv sync`, `uv add`, `uv run`)."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:twine|flit)(?:\\.exe)?\\b"
      flags: i
      tool: bash
      message: "Python publish/build tooling is disabled. Load the active language skill selected from the task artifacts and repository, then use uv workflow (`uv build`, `uv publish`)."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:pythonw?(?:[23])?(?:\\.\\d+)*|py|pypy3?)\\b[^\\n;|&]*(?:\\s-m\\s*(?:pip|venv|virtualenv|ensurepip|py_compile|compileall|build|twine)\\b|\\s-m(?:pip|venv|virtualenv|ensurepip|py_compile|compileall|build|twine)\\b)"
      flags: i
      tool: bash
      message: "`python -m ...` Python tooling is disabled. Load the active language skill selected from the task artifacts and repository, then use uv workflow (`uv run`, `uv pip`, `uv build`, `uv publish`)."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:pythonw?(?:[23])?(?:\\.\\d+)*|py|pypy3?)\\b[^\\n;|&]*\\bsetup\\.py\\b"
      flags: i
      tool: bash
      message: "`python setup.py ...` is disabled. Load the active language skill selected from the task artifacts and repository, then use PEP 517/uv workflow (`uv build`, `uv publish`, `uv sync`)."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:pytest|py\\.test|tox|nox|coverage|mypy|pyright|ruff|black|isort|flake8|pylint|ipython|jupyter|jupyter-lab|jupyter-notebook|notebook)(?:\\.exe)?\\b"
      flags: i
      tool: bash
      message: "Python CLI tools must run through uv. Load the active language skill selected from the task artifacts and repository, then use `uv run <tool>`, `uvx <tool>`, or `uv tool run <tool>`."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:pwsh|pwsh\\.exe|powershell|powershell\\.exe)\\b[^\\n]*\\s-(?:e|en|enc|encodedcommand)\\b"
      flags: i
      tool: bash
      message: "Opaque PowerShell payloads are blocked. Use an explicit script with `-Command`, or write a temporary `.ps1` under PowerShell temp and run it with `-File`."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:pwsh|pwsh\\.exe|powershell|powershell\\.exe)\\b(?![^\\n]*\\s-NoProfile\\b[^\\n]*\\s-(?:Command|c)\\b)[^\\n]*\\s-(?:Command|c)\\b"
      flags: i
      tool: bash
      message: "PowerShell from OMP bash must include `-NoProfile`. Use `pwsh.exe -NoProfile -NoLogo -NonInteractive -Command '...'`, or a temporary `.ps1` under PowerShell temp with `-File` for multiline scripts."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:pwsh|pwsh\\.exe|powershell|powershell\\.exe)\\b(?![^\\n]*\\s-NonInteractive\\b[^\\n]*\\s-(?:Command|c)\\b)[^\\n]*\\s-(?:Command|c)\\b"
      flags: i
      tool: bash
      message: "PowerShell from OMP bash must include `-NonInteractive`. Use `pwsh.exe -NoProfile -NoLogo -NonInteractive -Command '...'`, or a temporary `.ps1` under PowerShell temp with `-File` for multiline scripts."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:pwsh|pwsh\\.exe|powershell|powershell\\.exe)\\b[^\\n]*\\s-(?:Command|c)\\s+\""
      flags: i
      tool: bash
      message: "Do not wrap PowerShell `-Command` in Bash double quotes. Bash will eat `$`, backticks, and backslashes. Use single quotes around the script, or write a temporary `.ps1` under PowerShell temp and run it with `-File`."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:sudo\\s+|env\\s+)*(?:[A-Za-z_][A-Za-z0-9_]*=\\S+\\s+)*(?:cp|mv|rm|touch|mkdir)\\b[^\\n]*(?:[A-Za-z]:\\\\|\\\\\\\\)"
      flags: i
      tool: bash
      message: "Do not aim POSIX shell commands at Windows `C:\\...` or UNC paths. Use OMP `read`/`search`/`find`/`edit`/`write`, or run PowerShell with `-LiteralPath`."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:Start-Process|Invoke-Item|explorer(?:\\.exe)?|mshta(?:\\.exe)?|rundll32(?:\\.exe)?)\\b|(?:^|[;&|]\\s*)\\s*(?:pwsh|pwsh\\.exe|powershell|powershell\\.exe)\\b[^\\n]*\\s-(?:Command|c)\\s+[^\\n]*(?:Start-Process|Invoke-Item|explorer(?:\\.exe)?|mshta(?:\\.exe)?|rundll32(?:\\.exe)?)\\b|(?:^|[;&|]\\s*)\\s*cmd(?:\\.exe)?\\s+/c\\s+start\\b"
      flags: i
      tool: bash
      message: "GUI and URL launches from shell are blocked here. If you really need that, make the intent explicit and use the right tool or user-approved flow."
    - pattern: "\\b(?:Remove-Item\\b[^\\n]*\\s-Force\\b|Remove-Item\\b[^\\n]*\\s-Recurse\\b|cmd(?:\\.exe)?\\b[^\\n]*\\b(?:del|erase)\\b[^\\n]*\\s/[Ff]\\b|cmd(?:\\.exe)?\\b[^\\n]*\\b(?:rd|rmdir)\\b[^\\n]*\\s/[Ss]\\b[^\\n]*\\s/[Qq]\\b)"
      flags: i
      tool: bash
      message: "Forceful Windows delete patterns are blocked. Use OMP file tools for precise changes, or restate the destructive intent explicitly first."
    - pattern: "(?:^|[;&|]\\s*)\\s*(?:Get-ChildItem|Get-Content|Set-Location|Copy-Item|Move-Item|Remove-Item|Rename-Item|New-Item|Set-Content|Add-Content|Out-File|Select-String|Where-Object|ForEach-Object|Test-Path|Resolve-Path|Get-Item|Select-Object|Measure-Object)\\b"
      flags: i
      tool: bash
      message: "This is PowerShell syntax, not Bash. In OMP bash on Windows, invoke `pwsh.exe -NoProfile -NoLogo -NonInteractive -Command '[Console]::OutputEncoding=[System.Text.Encoding]::UTF8; <script>'`, or use a temporary `.ps1` under PowerShell temp with `-File` for quote-heavy commands."
bash.autoBackground.thresholdMs: 60000

# =============================================================================
# Integrations / Providers
# =============================================================================
github:
  enabled: true
disabledProviders:
  - claude
  - claude-plugins
  - cline
  - codex
  - cursor
  - gemini
  - mcp-json
  - opencode
  - vscode
  - github
  - windsurf

# =============================================================================
# Misc
# =============================================================================
commands:
  enableClaudeProject: false
  enableClaudeUser: false
secrets:
  enabled: true
startup:
  checkUpdate: false
providers:
  webSearch: brave
  webSearchExclude:
    - perplexity
    - anthropic
    - codex
    - xai
    - zai
    - tinyfish
    - jina
    - kagi
    - tavily
    - kimi
    - synthetic
    - searxng
    - startpage
    - duckduckgo
    - ecosia
    - google
    - mojeek
    - public
symbolPreset: unicode
setupVersion: 1
