diff --git a/src/features/authFiles/components/AuthFileCard.tsx b/src/features/authFiles/components/AuthFileCard.tsx
index fba1156..a605e79 100644
--- a/src/features/authFiles/components/AuthFileCard.tsx
+++ b/src/features/authFiles/components/AuthFileCard.tsx
@@ -14,11 +14,9 @@ import {
 } from '@/components/ui/icons';
 import { ProviderStatusBar } from '@/components/providers/ProviderStatusBar';
 import type { AuthFileItem } from '@/types';
-import { resolveAuthProvider } from '@/utils/quota';
 import { statusBarDataFromRecentRequests } from '@/utils/recentRequests';
 import { formatFileSize } from '@/utils/format';
 import {
-  QUOTA_PROVIDER_TYPES,
   formatModified,
   getAuthFileIcon,
   getAuthFileStatusMessage,
@@ -33,6 +31,7 @@ import {
   type QuotaProviderType,
   type ResolvedTheme,
 } from '@/features/authFiles/constants';
+import { resolveQuotaProviderType } from '@/features/quota/logic';
 import { deriveAuthFileIdentity } from '@/features/authFiles/identity';
 import type { AuthFileStatusBarData } from '@/features/authFiles/hooks/useAuthFilesStatusBarCache';
 import { AuthFileQuotaSection } from '@/features/authFiles/components/AuthFileQuotaSection';
@@ -60,11 +59,8 @@ export type AuthFileCardProps = {
   onToggleSelect: (name: string) => void;
 };
 
-const resolveQuotaType = (file: AuthFileItem): QuotaProviderType | null => {
-  const provider = resolveAuthProvider(file);
-  if (!QUOTA_PROVIDER_TYPES.has(provider as QuotaProviderType)) return null;
-  return provider as QuotaProviderType;
-};
+const resolveQuotaType = (file: AuthFileItem): QuotaProviderType | null =>
+  resolveQuotaProviderType(file);
 
 export function AuthFileCard(props: AuthFileCardProps) {
   const { t } = useTranslation();
@@ -103,7 +99,7 @@ export function AuthFileCard(props: AuthFileCardProps) {
 
   const quotaType =
     quotaFilterType && resolveQuotaType(file) === quotaFilterType ? quotaFilterType : null;
-  const showQuotaLayout = Boolean(quotaType) && !isRuntimeOnly && !compact;
+  const showQuotaLayout = Boolean(quotaType) && !compact;
 
   const successCount = file.successCount ?? 0;
   const failureCount = file.failureCount ?? 0;
diff --git a/src/features/authFiles/components/AuthFileQuotaSection.tsx b/src/features/authFiles/components/AuthFileQuotaSection.tsx
index f958cad..7fd4ad1 100644
--- a/src/features/authFiles/components/AuthFileQuotaSection.tsx
+++ b/src/features/authFiles/components/AuthFileQuotaSection.tsx
@@ -47,6 +47,8 @@ export function AuthFileQuotaSection(props: AuthFileQuotaSectionProps) {
     if (quotaType === 'codex') return state.codexQuota[file.name] as QuotaCardState | undefined;
     if (quotaType === 'kimi') return state.kimiQuota[file.name] as QuotaCardState | undefined;
     if (quotaType === 'xai') return state.xaiQuota[file.name] as QuotaCardState | undefined;
+    if (quotaType === 'custom')
+      return state.customQuota[file.name] as QuotaCardState | undefined;
     return assertNever(quotaType);
   });
 
@@ -56,7 +58,6 @@ export function AuthFileQuotaSection(props: AuthFileQuotaSectionProps) {
 
   const refreshQuotaForFile = useCallback(async () => {
     if (disableControls) return;
-    if (isRuntimeOnlyAuthFile(file)) return;
     if (file.disabled) return;
     if (quota?.status === 'loading') return;
 
diff --git a/src/features/authFiles/constants.ts b/src/features/authFiles/constants.ts
index 8d72494..f660907 100644
--- a/src/features/authFiles/constants.ts
+++ b/src/features/authFiles/constants.ts
@@ -24,7 +24,8 @@ export type AuthFileModelItem = {
 };
 export type AuthFileIconAsset = string | { light: string; dark: string };
 
-export type QuotaProviderType = 'antigravity' | 'claude' | 'codex' | 'kimi' | 'xai';
+export type QuotaProviderType =
+  'antigravity' | 'claude' | 'codex' | 'kimi' | 'xai' | 'custom';
 export type OAuthConfigLoadError = 'loading' | 'unsupported' | 'load' | null;
 
 export const QUOTA_PROVIDER_TYPES = new Set<QuotaProviderType>([
@@ -33,6 +34,7 @@ export const QUOTA_PROVIDER_TYPES = new Set<QuotaProviderType>([
   'codex',
   'kimi',
   'xai',
+  'custom',
 ]);
 
 export const OAUTH_PROVIDER_PRESETS = [
diff --git a/src/features/authFiles/hooks/useAuthFilesData.ts b/src/features/authFiles/hooks/useAuthFilesData.ts
index cab1cac..cd5bc34 100644
--- a/src/features/authFiles/hooks/useAuthFilesData.ts
+++ b/src/features/authFiles/hooks/useAuthFilesData.ts
@@ -1,6 +1,7 @@
 import { useCallback, useEffect, useRef, useState, type ChangeEvent, type RefObject } from 'react';
 import { useTranslation } from 'react-i18next';
-import { authFilesApi } from '@/services/api';
+import { authFilesApi, providersApi } from '@/services/api';
+import { mergeQuotaCompatAuthFiles } from '@/utils/quota';
 import { notifyAuthFilesChanged } from '@/features/authFiles/authFilesEvents';
 import { useNotificationStore } from '@/stores';
 import type { AuthFileItem } from '@/types';
@@ -200,9 +201,13 @@ export function useAuthFilesData(options?: UseAuthFilesDataOptions): UseAuthFile
       }
 
       try {
-        const data = await authFilesApi.list();
+        const [data, openaiCompat] = await Promise.all([
+          authFilesApi.list(),
+          providersApi.getOpenAIProviders().catch(() => []),
+        ]);
         if (requestId !== loadRequestIdRef.current) return; // 已被更新的请求/变更取代
-        setFiles(data?.files || []);
+        // Synthesize quota-capable API-key rows when they are absent from /auth-files.
+        setFiles(mergeQuotaCompatAuthFiles(data?.files || [], openaiCompat || []));
         setError('');
       } catch (err: unknown) {
         if (requestId !== loadRequestIdRef.current) return;
diff --git a/src/features/quota/QuotaPage.tsx b/src/features/quota/QuotaPage.tsx
index f1c07f3..4189911 100644
--- a/src/features/quota/QuotaPage.tsx
+++ b/src/features/quota/QuotaPage.tsx
@@ -10,7 +10,7 @@
 
 import { useCallback, useEffect, useMemo, useRef, useState } from 'react';
 import { useTranslation } from 'react-i18next';
-import { authFilesApi } from '@/services/api';
+import { authFilesApi, providersApi } from '@/services/api';
 import { Button } from '@/components/ui/Button';
 import { EmptyState } from '@/components/ui/EmptyState';
 import { Select } from '@/components/ui/Select';
@@ -20,6 +20,7 @@ import { useNow } from '@/hooks/useNow';
 import { useRevealGroup } from '@/hooks/motion';
 import { useAuthStore, useQuotaStore, useThemeStore } from '@/stores';
 import type { AuthFileItem, ResolvedTheme } from '@/types';
+import { mergeQuotaCompatAuthFiles } from '@/utils/quota';
 import { ProviderTabs } from '@/features/authFiles/components/ProviderTabs';
 import { QuotaHeader } from './components/QuotaHeader';
 import { QuotaCard } from './components/QuotaCard';
@@ -81,8 +82,13 @@ export function QuotaPage() {
     setLoading(true);
     setError('');
     try {
-      const data = await authFilesApi.list();
-      setFiles(data?.files || []);
+      const [data, openaiCompat] = await Promise.all([
+        authFilesApi.list(),
+        providersApi.getOpenAIProviders().catch(() => []),
+      ]);
+      const rawFiles = data?.files || [];
+      // Synthesize quota-capable API-key rows when they are absent from /auth-files.
+      setFiles(mergeQuotaCompatAuthFiles(rawFiles, openaiCompat || []));
     } catch (err: unknown) {
       const message = err instanceof Error ? err.message : t('notification.refresh_failed');
       setError(message);
@@ -105,6 +111,7 @@ export function QuotaPage() {
   const codexQuota = useQuotaStore((state) => state.codexQuota);
   const kimiQuota = useQuotaStore((state) => state.kimiQuota);
   const xaiQuota = useQuotaStore((state) => state.xaiQuota);
+  const customQuota = useQuotaStore((state) => state.customQuota);
 
   const quotaByType = useMemo<Record<QuotaProviderType, Record<string, QuotaCardState>>>(
     () =>
@@ -114,8 +121,9 @@ export function QuotaPage() {
         codex: codexQuota,
         kimi: kimiQuota,
         xai: xaiQuota,
+        custom: customQuota,
       }) as unknown as Record<QuotaProviderType, Record<string, QuotaCardState>>,
-    [antigravityQuota, claudeQuota, codexQuota, kimiQuota, xaiQuota]
+    [antigravityQuota, claudeQuota, codexQuota, kimiQuota, xaiQuota, customQuota]
   );
 
   const getQuota = useCallback(
diff --git a/src/features/quota/constants.ts b/src/features/quota/constants.ts
index ca29a05..9f83d2b 100644
--- a/src/features/quota/constants.ts
+++ b/src/features/quota/constants.ts
@@ -7,6 +7,7 @@ export const QUOTA_TAB_ORDER: readonly QuotaProviderType[] = [
   'codex',
   'xai',
   'kimi',
+  'custom',
 ];
 
 export type QuotaTabId = 'all' | QuotaProviderType;
diff --git a/src/features/quota/logic.ts b/src/features/quota/logic.ts
index 2b4b0ce..540f52e 100644
--- a/src/features/quota/logic.ts
+++ b/src/features/quota/logic.ts
@@ -9,6 +9,7 @@ import { CLAUDE_CONFIG } from './providers/claude/data';
 import { CODEX_CONFIG } from './providers/codex/data';
 import { KIMI_CONFIG } from './providers/kimi/data';
 import { XAI_CONFIG } from './providers/xai/data';
+import { CUSTOM_CONFIG } from './providers/custom/data';
 import type { QuotaProviderType } from './providers/types';
 import { QUOTA_TAB_ORDER, type QuotaSortMode, type QuotaTabId } from './constants';
 
@@ -18,6 +19,7 @@ const QUOTA_FILTER_MAP: Record<QuotaProviderType, (file: AuthFileItem) => boolea
   codex: CODEX_CONFIG.filterFn,
   kimi: KIMI_CONFIG.filterFn,
   xai: XAI_CONFIG.filterFn,
+  custom: CUSTOM_CONFIG.filterFn,
 };
 
 export interface QuotaFileEntry {
diff --git a/src/features/quota/providers/custom/CustomQuotaBody.tsx b/src/features/quota/providers/custom/CustomQuotaBody.tsx
new file mode 100644
index 0000000..fb605e4
--- /dev/null
+++ b/src/features/quota/providers/custom/CustomQuotaBody.tsx
@@ -0,0 +1,58 @@
+import { useMemo } from 'react';
+import { useTranslation } from 'react-i18next';
+import type { CustomQuotaState } from '@/types';
+import { buildResetDisplay } from '@/utils/quota';
+import { useNow } from '@/hooks/useNow';
+import { QuotaMeter } from '../../components/QuotaMeter';
+import { QuotaResetLabel } from '../../components/QuotaResetLabel';
+import { collectQuotaRowInstants, pickUrgentRowId } from '../../resetSchedule';
+import type { QuotaBodyProps } from '../../types';
+
+export function CustomQuotaBody({ quota, classes }: QuotaBodyProps<CustomQuotaState>) {
+  const { t, i18n } = useTranslation();
+  const now = useNow();
+  const soonestRowId = useMemo(
+    () => pickUrgentRowId(collectQuotaRowInstants('custom', quota), now),
+    [quota, now]
+  );
+  const windows = quota.windows ?? [];
+
+  if (windows.length === 0) {
+    return <div className={classes.quotaMessage}>{t('custom_quota.empty_data')}</div>;
+  }
+
+  return (
+    <>
+      {windows.map((window, index) => {
+        const remaining = window.remainingPercent;
+        const resetDisplay = buildResetDisplay(
+          window.resetLabel || null,
+          window.resetAtMs,
+          now,
+          i18n.resolvedLanguage
+        );
+        const soon = window.id === soonestRowId;
+        return (
+          <div
+            key={window.id}
+            className={classes.quotaRow}
+            title={soon ? t('quota_management.soonest_row_hint') : undefined}
+          >
+            <div className={classes.quotaRowHeader}>
+              <span className={classes.quotaModel}>{window.label}</span>
+              <div className={classes.quotaMeta}>
+                <span className={classes.quotaPercent}>
+                  {remaining === null ? '--' : `${Math.round(remaining)}%`}
+                </span>
+                {resetDisplay && (
+                  <QuotaResetLabel display={resetDisplay} classes={classes} soon={soon} />
+                )}
+              </div>
+            </div>
+            <QuotaMeter percent={remaining} classes={classes} index={index} />
+          </div>
+        );
+      })}
+    </>
+  );
+}
diff --git a/src/features/quota/providers/custom/data.ts b/src/features/quota/providers/custom/data.ts
new file mode 100644
index 0000000..c7bcb11
--- /dev/null
+++ b/src/features/quota/providers/custom/data.ts
@@ -0,0 +1,86 @@
+/**
+ * Generic quota provider: all cards defined by `@/utils/quota/custom/cards`.
+ */
+
+import type { TFunction } from 'i18next';
+import type { AuthFileItem, CustomQuotaState, CustomQuotaWindow } from '@/types';
+import { apiCallApi, getApiCallErrorMessage } from '@/services/api';
+import { createStatusError, isDisabledAuthFile } from '@/utils/quota';
+import { getQuotaCard } from '@/utils/quota/custom';
+import { normalizeAuthIndex } from '@/utils/authIndex';
+import type { QuotaProviderData } from '../types';
+
+export type CustomQuotaData = {
+  cardId: string;
+  cardTitle: string;
+  windows: CustomQuotaWindow[];
+};
+
+const mapStatusError = (status: number, fallback: string, t: TFunction) => {
+  if (status === 401) return t('custom_quota.unauthorized');
+  if (status === 403) return t('custom_quota.subscription_required');
+  if (status === 429) return t('custom_quota.rate_limited');
+  if (status >= 500) return t('custom_quota.upstream_unavailable');
+  return fallback;
+};
+
+const fetchCustomQuota = async (
+  file: AuthFileItem,
+  t: TFunction
+): Promise<CustomQuotaData> => {
+  const card = getQuotaCard(file);
+  if (!card) throw new Error(t('custom_quota.no_card'));
+
+  const authIndex = normalizeAuthIndex(file['auth_index'] ?? file.authIndex);
+  if (!authIndex) throw new Error(t('custom_quota.missing_auth_index'));
+
+  let result;
+  try {
+    result = await apiCallApi.request({ authIndex, ...card.request(file) });
+  } catch (err: unknown) {
+    const message = err instanceof Error ? err.message : t('custom_quota.request_failed');
+    throw createStatusError(
+      t('custom_quota.request_failed_with_message', { message }),
+      undefined
+    );
+  }
+
+  if (result.statusCode < 200 || result.statusCode >= 300) {
+    const fallback = getApiCallErrorMessage(result);
+    throw createStatusError(
+      mapStatusError(result.statusCode, fallback, t),
+      result.statusCode
+    );
+  }
+
+  const windows = card.parse(result.body ?? result.bodyText);
+  if (!windows || windows.length === 0) {
+    throw new Error(t('custom_quota.invalid_response'));
+  }
+  if (!windows.some((window) => window.remainingPercent !== null || window.resetAtMs != null)) {
+    throw new Error(t('custom_quota.empty_data'));
+  }
+  return { cardId: card.id, cardTitle: card.title, windows };
+};
+
+export const CUSTOM_CONFIG: QuotaProviderData<CustomQuotaState, CustomQuotaData> = {
+  type: 'custom',
+  i18nPrefix: 'custom_quota',
+  filterFn: (file) => getQuotaCard(file) !== undefined && !isDisabledAuthFile(file),
+  fetchQuota: fetchCustomQuota,
+  storeSelector: (state) => state.customQuota,
+  storeSetter: 'setCustomQuota',
+  buildLoadingState: () => ({ status: 'loading', windows: [] }),
+  buildSuccessState: (data) => ({
+    status: 'success',
+    windows: data.windows,
+    cardId: data.cardId,
+    cardTitle: data.cardTitle,
+  }),
+  buildErrorState: (message, status) => ({
+    status: 'error',
+    windows: [],
+    error: message,
+    errorStatus: status,
+  }),
+};
diff --git a/src/features/quota/providers/index.ts b/src/features/quota/providers/index.ts
index b24942c..4c14360 100644
--- a/src/features/quota/providers/index.ts
+++ b/src/features/quota/providers/index.ts
@@ -21,6 +21,8 @@ import { KIMI_CONFIG } from './kimi/data';
 import { KimiQuotaBody } from './kimi/KimiQuotaBody';
 import { XAI_CONFIG } from './xai/data';
 import { XaiQuotaBody } from './xai/XaiQuotaBody';
+import { CUSTOM_CONFIG } from './custom/data';
+import { CustomQuotaBody } from './custom/CustomQuotaBody';
 
 /** 所有 provider 额度状态的公共骨架（各 *QuotaState 的结构子集）。 */
 export interface QuotaCardState {
@@ -53,6 +55,7 @@ export const QUOTA_ADAPTERS: Record<QuotaProviderType, QuotaAdapter> = {
   codex: { ...CODEX_CONFIG, Body: CodexQuotaBody } as unknown as QuotaAdapter,
   kimi: { ...KIMI_CONFIG, Body: KimiQuotaBody } as unknown as QuotaAdapter,
   xai: { ...XAI_CONFIG, Body: XaiQuotaBody } as unknown as QuotaAdapter,
+  custom: { ...CUSTOM_CONFIG, Body: CustomQuotaBody } as unknown as QuotaAdapter,
 };
 
 export type QuotaMapUpdater = (
diff --git a/src/features/quota/providers/types.ts b/src/features/quota/providers/types.ts
index 2ef0270..1c0fd1e 100644
--- a/src/features/quota/providers/types.ts
+++ b/src/features/quota/providers/types.ts
@@ -11,13 +11,15 @@ import type {
   AuthFileItem,
   ClaudeQuotaState,
   CodexQuotaState,
+  CustomQuotaState,
   KimiQuotaState,
   XaiQuotaState,
 } from '@/types';
 
 export type QuotaUpdater<T> = T | ((prev: T) => T);
 
-export type QuotaProviderType = 'antigravity' | 'claude' | 'codex' | 'kimi' | 'xai';
+export type QuotaProviderType =
+  'antigravity' | 'claude' | 'codex' | 'kimi' | 'xai' | 'custom';
 
 /** useQuotaStore 的结构契约（storeSelector/storeSetter 依赖）。 */
 export interface QuotaStore {
@@ -26,11 +28,13 @@ export interface QuotaStore {
   codexQuota: Record<string, CodexQuotaState>;
   kimiQuota: Record<string, KimiQuotaState>;
   xaiQuota: Record<string, XaiQuotaState>;
+  customQuota: Record<string, CustomQuotaState>;
   setAntigravityQuota: (updater: QuotaUpdater<Record<string, AntigravityQuotaState>>) => void;
   setClaudeQuota: (updater: QuotaUpdater<Record<string, ClaudeQuotaState>>) => void;
   setCodexQuota: (updater: QuotaUpdater<Record<string, CodexQuotaState>>) => void;
   setKimiQuota: (updater: QuotaUpdater<Record<string, KimiQuotaState>>) => void;
   setXaiQuota: (updater: QuotaUpdater<Record<string, XaiQuotaState>>) => void;
+  setCustomQuota: (updater: QuotaUpdater<Record<string, CustomQuotaState>>) => void;
   clearQuotaCache: () => void;
 }
 
diff --git a/src/features/quota/quotaTimelineModel.ts b/src/features/quota/quotaTimelineModel.ts
index c9012a7..f6ddcab 100644
--- a/src/features/quota/quotaTimelineModel.ts
+++ b/src/features/quota/quotaTimelineModel.ts
@@ -349,7 +349,7 @@ export function buildTimelineLane(input: TimelineLaneInput): TimelineLane {
 
   if (!quota || quota.status !== 'success') return empty;
 
-  if (provider === 'claude' || provider === 'codex') {
+  if (provider === 'claude' || provider === 'codex' || provider === 'custom') {
     const windows = ((quota as { windows?: WindowLike[] }).windows ?? []).filter(
       (window) => typeof window.resetAtMs === 'number'
     );
diff --git a/src/features/quota/resetSchedule.ts b/src/features/quota/resetSchedule.ts
index abdb7e1..ea3d6d0 100644
--- a/src/features/quota/resetSchedule.ts
+++ b/src/features/quota/resetSchedule.ts
@@ -90,9 +90,9 @@ export function collectQuotaRowInstants(
   const state = quota as { status?: string } | undefined;
   if (!state || state.status !== 'success') return [];
 
-  if (provider === 'claude' || provider === 'codex') {
+  if (provider === 'claude' || provider === 'codex' || provider === 'custom') {
     const windows = collectRows((quota as { windows?: WindowLike[] }).windows ?? [], 'window');
-    if (provider !== 'codex') return windows;
+    if (provider !== 'codex') return windows; // custom / claude: windows only
 
     const credits = (
       (quota as { rateLimitResetCredits?: ResetCreditLike[] }).rateLimitResetCredits ?? []
diff --git a/src/i18n/locales/en.json b/src/i18n/locales/en.json
index 833e0de..9bf1399 100644
--- a/src/i18n/locales/en.json
+++ b/src/i18n/locales/en.json
@@ -365,7 +365,9 @@
     "quota_refresh_single": "Refresh quota",
     "quota_refresh_hint": "Refresh quota only for this credential",
     "quota_refresh_success": "Quota refreshed for \"{{name}}\"",
-    "quota_refresh_failed": "Failed to refresh quota for \"{{name}}\": {{message}}"
+    "quota_refresh_failed": "Failed to refresh quota for \"{{name}}\": {{message}}",
+    "filter_opencode": "OpenCode Go",
+    "filter_cline": "ClinePass"
   },
   "antigravity_subscription": {
     "plan_unknown": "Unknown",
@@ -1171,7 +1173,7 @@
   },
   "quota_management": {
     "title": "Quota Management",
-    "description": "Monitor OAuth quota status for Antigravity, Codex, Claude, Kimi, and xAI credentials.",
+    "description": "Monitor quota status for Antigravity, Codex, Claude, Kimi, xAI, OpenCode Go, and ClinePass credentials.",
     "refresh_all_credentials": "Refresh all credentials",
     "sort_label": "Sort",
     "sort_default": "Default",
@@ -1180,7 +1182,7 @@
     "meta_loaded": "{{count}} loaded",
     "meta_attention": "{{count}} need attention",
     "empty_title": "No quota-capable credentials",
-    "empty_desc": "Connect an Antigravity, Claude, Codex, xAI, or Kimi credential to track its quota here.",
+    "empty_desc": "Connect an Antigravity, Claude, Codex, xAI, Kimi, OpenCode Go, or ClinePass credential to track its quota here.",
     "windows_title": "Quota windows",
     "windows_span_weekly": "two weeks",
     "windows_span_session": "three days",
@@ -1738,5 +1740,23 @@
     "modelCatalog": {
       "notLoaded": "Not loaded"
     }
+  },
+  "custom_quota": {
+    "title": "Custom Quota",
+    "empty_title": "No custom-quota credentials",
+    "empty_desc": "Add a provider covered by a registered quota card to track it here.",
+    "idle": "Click here to refresh quota",
+    "loading": "Loading quota...",
+    "load_failed": "Failed to load quota: {{message}}",
+    "missing_auth_index": "Credential missing auth_index",
+    "no_card": "No quota card matches this credential",
+    "empty_data": "No quota data available",
+    "invalid_response": "Invalid quota response",
+    "unauthorized": "Credential unauthorized",
+    "subscription_required": "Subscription required",
+    "rate_limited": "Upstream rate limited",
+    "upstream_unavailable": "Upstream unavailable",
+    "request_failed": "Quota request failed",
+    "request_failed_with_message": "Quota request failed: {{message}}"
   }
 }
diff --git a/src/i18n/locales/ru.json b/src/i18n/locales/ru.json
index 622ff62..348dd26 100644
--- a/src/i18n/locales/ru.json
+++ b/src/i18n/locales/ru.json
@@ -364,7 +364,9 @@
     "quota_refresh_single": "Обновить квоту",
     "quota_refresh_hint": "Обновить квоту только для этих учётных данных",
     "quota_refresh_success": "Квота для \"{{name}}\" обновлена",
-    "quota_refresh_failed": "Не удалось обновить квоту для \"{{name}}\": {{message}}"
+    "quota_refresh_failed": "Не удалось обновить квоту для \"{{name}}\": {{message}}",
+    "filter_opencode": "OpenCode Go",
+    "filter_cline": "ClinePass"
   },
   "antigravity_subscription": {
     "plan_unknown": "Неизвестно",
@@ -1170,7 +1172,7 @@
   },
   "quota_management": {
     "title": "Управление квотами",
-    "description": "Следите за статусом квот OAuth для учётных данных Antigravity, Codex, Claude, Kimi и xAI.",
+    "description": "Мониторинг квот Antigravity, Codex, Claude, Kimi, xAI и OpenCode Go.",
     "refresh_all_credentials": "Обновить все учётные данные",
     "sort_label": "Сортировка",
     "sort_default": "По умолчанию",
@@ -1179,7 +1181,7 @@
     "meta_loaded": "{{count}} загружено",
     "meta_attention": "{{count}} требуют внимания",
     "empty_title": "Нет учётных данных с квотами",
-    "empty_desc": "Подключите учётные данные Antigravity, Claude, Codex, xAI или Kimi, чтобы отслеживать квоту здесь.",
+    "empty_desc": "Подключите Antigravity, Claude, Codex, xAI, Kimi или OpenCode Go, чтобы отслеживать квоту здесь.",
     "windows_title": "Окна квот",
     "windows_span_weekly": "две недели",
     "windows_span_session": "три дня",
@@ -1728,5 +1730,23 @@
     "modelCatalog": {
       "notLoaded": "Не загружено"
     }
+  },
+  "custom_quota": {
+    "title": "Пользовательская квота",
+    "empty_title": "Нет учётных данных",
+    "empty_desc": "Добавьте провайдера, покрытого зарегистрированной картой квоты.",
+    "idle": "Нажмите, чтобы обновить квоту",
+    "loading": "Загрузка квоты...",
+    "load_failed": "Не удалось загрузить квоту: {{message}}",
+    "missing_auth_index": "В учётных данных нет auth_index",
+    "no_card": "Ни одна карта квоты не подходит к этим учётным данным",
+    "empty_data": "Нет данных о квоте",
+    "invalid_response": "Некорректный ответ о квоте",
+    "unauthorized": "Учётные данные не авторизованы",
+    "subscription_required": "Требуется подписка",
+    "rate_limited": "Upstream ограничил частоту запросов",
+    "upstream_unavailable": "Upstream недоступен",
+    "request_failed": "Запрос квоты не выполнен",
+    "request_failed_with_message": "Запрос квоты не выполнен: {{message}}"
   }
 }
diff --git a/src/i18n/locales/zh-CN.json b/src/i18n/locales/zh-CN.json
index c45f3cc..443a944 100644
--- a/src/i18n/locales/zh-CN.json
+++ b/src/i18n/locales/zh-CN.json
@@ -365,7 +365,9 @@
     "quota_refresh_single": "刷新额度",
     "quota_refresh_hint": "仅刷新该认证文件的额度",
     "quota_refresh_success": "已刷新 \"{{name}}\" 的额度",
-    "quota_refresh_failed": "刷新 \"{{name}}\" 的额度失败：{{message}}"
+    "quota_refresh_failed": "刷新 \"{{name}}\" 的额度失败：{{message}}",
+    "filter_opencode": "OpenCode Go",
+    "filter_cline": "ClinePass"
   },
   "antigravity_subscription": {
     "plan_unknown": "未知",
@@ -1171,7 +1173,7 @@
   },
   "quota_management": {
     "title": "配额管理",
-    "description": "集中查看 OAuth 额度与剩余情况",
+    "description": "监控 Antigravity、Codex、Claude、Kimi、xAI 与 OpenCode Go 凭证的额度状态。",
     "refresh_all_credentials": "刷新全部凭证",
     "sort_label": "排序",
     "sort_default": "默认",
@@ -1180,7 +1182,7 @@
     "meta_loaded": "{{count}} 个已加载",
     "meta_attention": "{{count}} 个需关注",
     "empty_title": "暂无可查额度的凭证",
-    "empty_desc": "接入 Antigravity、Claude、Codex、xAI 或 Kimi 凭证后即可在此查看额度。",
+    "empty_desc": "连接 Antigravity、Claude、Codex、xAI、Kimi 或 OpenCode Go 凭证后可在此跟踪额度。",
     "windows_title": "配额窗口",
     "windows_span_weekly": "两周",
     "windows_span_session": "三天",
@@ -1738,5 +1740,23 @@
     "modelCatalog": {
       "notLoaded": "未加载"
     }
+  },
+  "custom_quota": {
+    "title": "自定义额度",
+    "empty_title": "没有自定义额度凭证",
+    "empty_desc": "添加由已注册额度卡覆盖的提供商后可在此跟踪。",
+    "idle": "点击此处刷新额度",
+    "loading": "正在加载额度...",
+    "load_failed": "加载额度失败：{{message}}",
+    "missing_auth_index": "凭证缺少 auth_index",
+    "no_card": "没有匹配此凭证的额度卡",
+    "empty_data": "暂无额度数据",
+    "invalid_response": "额度响应无效",
+    "unauthorized": "凭证未授权",
+    "subscription_required": "需要订阅",
+    "rate_limited": "上游限流",
+    "upstream_unavailable": "上游不可用",
+    "request_failed": "额度请求失败",
+    "request_failed_with_message": "额度请求失败：{{message}}"
   }
 }
diff --git a/src/i18n/locales/zh-TW.json b/src/i18n/locales/zh-TW.json
index 1da9fb3..7263d58 100644
--- a/src/i18n/locales/zh-TW.json
+++ b/src/i18n/locales/zh-TW.json
@@ -365,7 +365,9 @@
     "quota_refresh_single": "重新整理配額",
     "quota_refresh_hint": "僅重新整理該驗證檔案的配額",
     "quota_refresh_success": "已重新整理「{{name}}」的配額",
-    "quota_refresh_failed": "重新整理「{{name}}」的配額失敗：{{message}}"
+    "quota_refresh_failed": "重新整理「{{name}}」的配額失敗：{{message}}",
+    "filter_opencode": "OpenCode Go",
+    "filter_cline": "ClinePass"
   },
   "antigravity_subscription": {
     "plan_unknown": "未知",
@@ -1197,7 +1199,7 @@
   },
   "quota_management": {
     "title": "配額管理",
-    "description": "集中查看 OAuth 配額與剩餘情況",
+    "description": "監控 Antigravity、Codex、Claude、Kimi、xAI 與 OpenCode Go 憑證的額度狀態。",
     "refresh_all_credentials": "重新整理全部憑證",
     "sort_label": "排序",
     "sort_default": "預設",
@@ -1206,7 +1208,7 @@
     "meta_loaded": "{{count}} 個已載入",
     "meta_attention": "{{count}} 個需關注",
     "empty_title": "暫無可查配額的憑證",
-    "empty_desc": "接入 Antigravity、Claude、Codex、xAI 或 Kimi 憑證後即可在此查看配額。",
+    "empty_desc": "連接 Antigravity、Claude、Codex、xAI、Kimi 或 OpenCode Go 憑證後可在此追蹤額度。",
     "windows_title": "配額視窗",
     "windows_span_weekly": "兩週",
     "windows_span_session": "三天",
@@ -1764,5 +1766,23 @@
     "modelCatalog": {
       "notLoaded": "未載入"
     }
+  },
+  "custom_quota": {
+    "title": "自訂額度",
+    "empty_title": "沒有自訂額度憑證",
+    "empty_desc": "新增由已註冊額度卡涵蓋的提供者後可在此追蹤。",
+    "idle": "點此重新整理額度",
+    "loading": "正在載入額度...",
+    "load_failed": "載入額度失敗：{{message}}",
+    "missing_auth_index": "憑證缺少 auth_index",
+    "no_card": "沒有符合此憑證的額度卡",
+    "empty_data": "暫無額度資料",
+    "invalid_response": "額度回應無效",
+    "unauthorized": "憑證未授權",
+    "subscription_required": "需要訂閱",
+    "rate_limited": "上游限流",
+    "upstream_unavailable": "上游無法使用",
+    "request_failed": "額度請求失敗",
+    "request_failed_with_message": "額度請求失敗：{{message}}"
   }
 }
diff --git a/src/stores/useQuotaStore.ts b/src/stores/useQuotaStore.ts
index 5072099..ec380e3 100644
--- a/src/stores/useQuotaStore.ts
+++ b/src/stores/useQuotaStore.ts
@@ -7,6 +7,7 @@ import type {
   AntigravityQuotaState,
   ClaudeQuotaState,
   CodexQuotaState,
+  CustomQuotaState,
   KimiQuotaState,
   XaiQuotaState,
 } from '@/types';
@@ -20,11 +21,13 @@ interface QuotaStoreState {
   codexQuota: Record<string, CodexQuotaState>;
   kimiQuota: Record<string, KimiQuotaState>;
   xaiQuota: Record<string, XaiQuotaState>;
+  customQuota: Record<string, CustomQuotaState>;
   setAntigravityQuota: (updater: QuotaUpdater<Record<string, AntigravityQuotaState>>) => void;
   setClaudeQuota: (updater: QuotaUpdater<Record<string, ClaudeQuotaState>>) => void;
   setCodexQuota: (updater: QuotaUpdater<Record<string, CodexQuotaState>>) => void;
   setKimiQuota: (updater: QuotaUpdater<Record<string, KimiQuotaState>>) => void;
   setXaiQuota: (updater: QuotaUpdater<Record<string, XaiQuotaState>>) => void;
+  setCustomQuota: (updater: QuotaUpdater<Record<string, CustomQuotaState>>) => void;
   clearQuotaCache: () => void;
 }
 
@@ -42,6 +45,7 @@ export const useQuotaStore = create<QuotaStoreState>((set) => ({
   codexQuota: {},
   kimiQuota: {},
   xaiQuota: {},
+  customQuota: {},
   setAntigravityQuota: (updater) =>
     set((state) => ({
       antigravityQuota: resolveUpdater(updater, state.antigravityQuota),
@@ -62,6 +66,10 @@ export const useQuotaStore = create<QuotaStoreState>((set) => ({
     set((state) => ({
       xaiQuota: resolveUpdater(updater, state.xaiQuota),
     })),
+  setCustomQuota: (updater) =>
+    set((state) => ({
+      customQuota: resolveUpdater(updater, state.customQuota),
+    })),
   clearQuotaCache: () =>
     set((state) => ({
       cacheGeneration: state.cacheGeneration + 1,
@@ -70,6 +78,7 @@ export const useQuotaStore = create<QuotaStoreState>((set) => ({
       codexQuota: {},
       kimiQuota: {},
       xaiQuota: {},
+      customQuota: {},
     })),
 }));
 
diff --git a/src/types/quota.ts b/src/types/quota.ts
index 8472879..738308d 100644
--- a/src/types/quota.ts
+++ b/src/types/quota.ts
@@ -310,6 +310,23 @@ export interface KimiQuotaState {
   errorStatus?: number;
 }
 
+export interface CustomQuotaWindow {
+  id: string;
+  label: string;
+  remainingPercent: number | null;
+  resetLabel?: string | null;
+  resetAtMs?: number | null;
+}
+
+export interface CustomQuotaState {
+  status: 'idle' | 'loading' | 'success' | 'error';
+  windows: CustomQuotaWindow[];
+  cardId?: string;
+  cardTitle?: string;
+  error?: string;
+  errorStatus?: number;
+}
+
 // xAI/Grok API payload types
 export interface XaiBillingCent {
   val?: number | string;
diff --git a/src/utils/quota/compat.ts b/src/utils/quota/compat.ts
new file mode 100644
index 0000000..214677f
--- /dev/null
+++ b/src/utils/quota/compat.ts
@@ -0,0 +1,118 @@
+/**
+ * Quota-capable API-key compatibility rows.
+ *
+ * `/auth-files` does not expose compatibility-provider base URLs or per-key
+ * credentials, so quota cards that target those pools need synthesized rows
+ * carrying `auth_index` and `baseUrl`.
+ */
+
+import type { AuthFileItem } from '@/types';
+import { getCompatCardForBaseUrl } from './custom';
+
+type OpenAICompatProviderLike = {
+  name?: string;
+  baseUrl?: string;
+  disabled?: boolean;
+  apiKeyEntries?: Array<{ authIndex?: string; apiKey?: string }>;
+};
+
+const isRecord = (value: unknown): value is Record<string, unknown> =>
+  Boolean(value) && typeof value === 'object' && !Array.isArray(value);
+
+const readString = (value: unknown): string =>
+  typeof value === 'string' ? value.trim() : value == null ? '' : String(value).trim();
+
+const readBaseUrl = (record: Record<string, unknown>): string => {
+  for (const candidate of [
+    record.baseUrl,
+    record['base-url'],
+    record.base_url,
+    record.BaseURL,
+  ]) {
+    const value = readString(candidate);
+    if (value) return value;
+  }
+  return '';
+};
+
+const readAuthIndex = (record: Record<string, unknown>): string =>
+  readString(record.authIndex ?? record['auth_index']);
+
+/** Attach compatibility-provider base URLs onto auth-file rows by auth_index. */
+export function attachCompatBaseUrls(
+  files: AuthFileItem[],
+  compatProviders: OpenAICompatProviderLike[]
+): AuthFileItem[] {
+  const byAuthIndex = new Map<string, string>();
+  for (const provider of compatProviders) {
+    const baseUrl = readString(provider.baseUrl);
+    if (!baseUrl) continue;
+    for (const entry of provider.apiKeyEntries ?? []) {
+      const authIndex = readString(entry.authIndex);
+      if (!authIndex) continue;
+      byAuthIndex.set(authIndex, baseUrl);
+    }
+  }
+
+  if (byAuthIndex.size === 0) return files;
+
+  return files.map((file) => {
+    const existing = readBaseUrl(file as unknown as Record<string, unknown>);
+    if (existing) return file;
+    const authIndex = readAuthIndex(file as unknown as Record<string, unknown>);
+    if (!authIndex) return file;
+    const baseUrl = byAuthIndex.get(authIndex);
+    if (!baseUrl) return file;
+    return { ...file, baseUrl, 'base-url': baseUrl };
+  });
+}
+
+/**
+ * Synthesize quota-capable rows for compatibility providers that a registered
+ * card claims via `matchesBaseUrl` but that `/auth-files` does not list.
+ */
+export function mergeQuotaCompatAuthFiles(
+  files: AuthFileItem[],
+  compatProviders: OpenAICompatProviderLike[]
+): AuthFileItem[] {
+  const withBase = attachCompatBaseUrls(files, compatProviders);
+  const existingAuthIndexes = new Set<string>();
+  for (const file of withBase) {
+    const authIndex = readAuthIndex(file as unknown as Record<string, unknown>);
+    if (authIndex) existingAuthIndexes.add(authIndex);
+  }
+
+  const synthesized: AuthFileItem[] = [];
+  for (const provider of compatProviders) {
+    const baseUrl = readString(provider.baseUrl);
+    if (!baseUrl || provider.disabled === true) continue;
+    const card = getCompatCardForBaseUrl(baseUrl);
+    if (!card) continue;
+    const providerLike: unknown = provider;
+    const providerName = isRecord(providerLike)
+      ? readString(providerLike.name)
+      : '';
+    const label = providerName || card.compatTitle || card.title;
+    for (const entry of provider.apiKeyEntries ?? []) {
+      const authIndex = readString(entry.authIndex);
+      if (!authIndex || existingAuthIndexes.has(authIndex)) continue;
+      synthesized.push({
+        name: `openai-compat:${label}:${authIndex}`,
+        authIndex,
+        auth_index: authIndex,
+        baseUrl,
+        'base-url': baseUrl,
+        provider: 'openai-compatible',
+        type: 'openai-compatible',
+        label,
+        disabled: false,
+        source: 'openai-compatibility',
+        runtime_only: true,
+        runtimeOnly: true,
+      } as AuthFileItem);
+      existingAuthIndexes.add(authIndex);
+    }
+  }
+
+  return synthesized.length > 0 ? [...withBase, ...synthesized] : withBase;
+}
diff --git a/src/utils/quota/custom/cards.ts b/src/utils/quota/custom/cards.ts
new file mode 100644
index 0000000..8f02c43
--- /dev/null
+++ b/src/utils/quota/custom/cards.ts
@@ -0,0 +1,10 @@
+/**
+ * Quota-card definitions.
+ *
+ * This stub is replaced at build time with `tools/cliproxyapi/quota-cards.ts`
+ * from the agents repository (see `tools/cliproxyapi/panel.rebuild.sh`).
+ */
+
+import type { QuotaCard } from './types';
+
+export const QUOTA_CARDS: readonly QuotaCard[] = [];
diff --git a/src/utils/quota/custom/index.ts b/src/utils/quota/custom/index.ts
new file mode 100644
index 0000000..e56e76b
--- /dev/null
+++ b/src/utils/quota/custom/index.ts
@@ -0,0 +1,3 @@
+export * from './cards';
+export * from './match';
+export * from './types';
diff --git a/src/utils/quota/custom/match.ts b/src/utils/quota/custom/match.ts
new file mode 100644
index 0000000..cc950cf
--- /dev/null
+++ b/src/utils/quota/custom/match.ts
@@ -0,0 +1,21 @@
+/**
+ * Quota-card lookup helpers.
+ */
+
+import type { AuthFileItem } from '@/types';
+import { QUOTA_CARDS } from './cards';
+import type { QuotaCard } from './types';
+
+/** First registered card that matches an auth-file row, if any. */
+export const getQuotaCard = (file: AuthFileItem): QuotaCard | undefined =>
+  QUOTA_CARDS.find((card) => card.matches(file));
+
+/** Cards that can serve API-key compatibility providers. */
+export const getCompatCards = (): readonly QuotaCard[] =>
+  QUOTA_CARDS.filter((card) => card.matchesBaseUrl !== undefined);
+
+/** Card responsible for a compatibility provider base URL, if any. */
+export const getCompatCardForBaseUrl = (baseUrl: string): QuotaCard | undefined =>
+  getCompatCards().find((card) => card.matchesBaseUrl?.(baseUrl) === true);
+
+export type { QuotaCard };
diff --git a/src/utils/quota/custom/types.ts b/src/utils/quota/custom/types.ts
new file mode 100644
index 0000000..af10a5a
--- /dev/null
+++ b/src/utils/quota/custom/types.ts
@@ -0,0 +1,39 @@
+/**
+ * Declarative quota-card contract.
+ *
+ * Cards are data, not code: the panel ships one generic `custom` provider that
+ * consumes this contract, so adding a provider card means adding one object to
+ * `cards.ts` (see `tools/cliproxyapi/quota-cards.ts` in the agents repository).
+ */
+
+import type { CustomQuotaWindow } from '@/types';
+import type { AuthFileItem } from '@/types';
+
+export interface QuotaCardRequest {
+  method: string;
+  url: string;
+  /** `$TOKEN$` is replaced with the selected credential by the gateway. */
+  header?: Record<string, string>;
+  data?: string;
+}
+
+export interface QuotaCard {
+  /** Stable id; used for state and DOM keys. */
+  id: string;
+  /** Card title (provider name) shown on the quota page. */
+  title: string;
+  /** Auth-file rows this card serves. */
+  matches: (file: AuthFileItem) => boolean;
+  /**
+   * Base-URL matcher for API-key compatibility providers. Cards with this
+   * field get synthetic rows when the gateway lists the provider but not its
+   * per-key credentials.
+   */
+  matchesBaseUrl?: (baseUrl: string) => boolean;
+  /** Title used for synthesized compatibility rows. Defaults to `title`. */
+  compatTitle?: string;
+  /** Upstream request; `$TOKEN$` in headers or body is substituted by the gateway. */
+  request: (file: AuthFileItem) => QuotaCardRequest;
+  /** Map an api-call payload into windows; null or empty means "no data". */
+  parse: (payload: unknown) => CustomQuotaWindow[] | null;
+}
diff --git a/src/utils/quota/index.ts b/src/utils/quota/index.ts
index 83f973b..603fe7c 100644
--- a/src/utils/quota/index.ts
+++ b/src/utils/quota/index.ts
@@ -14,3 +14,5 @@ export * from './validators';
 export * from './builders';
 export * from './resetCredits';
 export * from './xaiPaid';
+export * from './compat';
+export * from './custom';
diff --git a/tests/quotaPageLogic.test.ts b/tests/quotaPageLogic.test.ts
index 2592bbc..7941dea 100644
--- a/tests/quotaPageLogic.test.ts
+++ b/tests/quotaPageLogic.test.ts
@@ -57,6 +57,7 @@ describe('buildTabCounts', () => {
       codex: 2,
       xai: 1,
       kimi: 1,
+      custom: 0,
     });
   });
 });
