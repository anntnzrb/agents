# Footer extension

Purpose: render custom footer with cwd, git branch, model, context usage, and statuses.

## File map
- index.ts: footer rendering logic

## Navigation
Start at index.ts (buildFooterLine).
