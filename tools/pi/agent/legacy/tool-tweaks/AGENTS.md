# Tool tweaks extension

Purpose: render minimal tool call/output in the UI.

## File map
- index.disabled.ts: tool override and renderers

## Navigation
Start at index.disabled.ts (renderHiddenCall).
